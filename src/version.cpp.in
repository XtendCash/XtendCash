#define DEF_XTEND_VERSION_MAJOR 3
#define DEF_XTEND_VERSION_MINOR 0
#define DEF_XTEND_VERSION_PATCH 5

#define XTEND_STRINGIFY2(val) #val
#define XTEND_STRINGIFY(val) XTEND_STRINGIFY2(val)
#define DEF_XTEND_VERSION XTEND_STRINGIFY(DEF_XTEND_VERSION_MAJOR) "." XTEND_STRINGIFY(DEF_XTEND_VERSION_MINOR) "." XTEND_STRINGIFY(DEF_XTEND_VERSION_PATCH)

#define DEF_XTEND_VERSION_TAG "@VERSIONTAG@"
#define DEF_XTEND_RELEASE_NAME "Merged Luck"
#define DEF_XTEND_VERSION_FULL DEF_XTEND_VERSION "-" DEF_XTEND_VERSION_TAG

#include "version.h"

const int XTEND_VERSION_MAJOR = DEF_XTEND_VERSION_MAJOR;
const int XTEND_VERSION_MINOR = DEF_XTEND_VERSION_MINOR;
const int XTEND_VERSION_PATCH = DEF_XTEND_VERSION_PATCH;

const char* const XTEND_VERSION_TAG = DEF_XTEND_VERSION_TAG;
const char* const XTEND_VERSION = DEF_XTEND_VERSION;
const char* const XTEND_RELEASE_NAME = DEF_XTEND_RELEASE_NAME;
const char* const XTEND_VERSION_FULL = DEF_XTEND_VERSION_FULL;
